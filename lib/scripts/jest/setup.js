import receiver from './receiver-api-v2';

window.cast = {
    receiver,
}

 window.OO = {
    STATE: {
        LOADING: 'loading',
        READY: 'ready',
        LOADING: 'loading',
        PAUSED: 'paused',
    },
    Player: {},
 };